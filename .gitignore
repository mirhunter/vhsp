# Real credentials — never commit
creds.md

# Python
__pycache__/
*.pyc
.venv/
*.egg-info/

# Node
node_modules/

# Local Claude Code settings
.claude/settings.local.json

# Host-specific rendered deploy artifacts (see deploy/vhsp-render).
# Never commit these -- they carry one deployment's real username/paths.
control-plane/deploy/rendered/

# pytest
.pytest_cache/
